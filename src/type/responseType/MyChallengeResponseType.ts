export interface MyChallengeResponseType {
    roomId?: number,
    balance?: number,
    title?: string,
    description?: string,
    imageUrl?: string,
    category?: string,
    goalAmount?: number,
}